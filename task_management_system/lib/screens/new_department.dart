import 'package:flutter/material.dart';

class NewDepartment extends StatelessWidget {
  const NewDepartment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
